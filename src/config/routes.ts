const routes = {
  admin: '/admin',
  deploy: '/deploy',
  initialize: '/initialize',
  whitelist: '/whitelist',
  add: '/addCar',
  settings: '/settings',
  authorize: '/authorize',
};

export default routes;
